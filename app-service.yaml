apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: /snap/kompose/19/kompose-linux-amd64 convert
    kompose.version: 1.21.0 (992df58d8)
  creationTimestamp: null
  labels:
    io.kompose.service: app
  name: app
spec:
  type: NodePort  # Cambiado de ClusterIP a NodePort
  ports:
  - name: "3000"
    port: 3000
    targetPort: 3000
    nodePort: 32000  # Opcional: especifica un puerto para acceder
  selector:
    io.kompose.service: app
status:
  loadBalancer: {}
